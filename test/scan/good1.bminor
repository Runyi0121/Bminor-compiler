x:integer = 65;
y:string = "hello";
z:boolean = false;
September:function integer ( a: integer, b: integer ) = {
	if (a < b) {
		return a;
	}
	else {
		return b;
	}
}
testfunc:function void ( z: integer ) = {
	y:integer = x * (x * 10 + 55 % 6 + z / 10);
	print "y: ", y, "\n";
	c:boolean = x < 100 && y > 200 ||  ! (z = true);
	print "c: ", c, "\n";
	d:boolean = y == "goodbye";
	print "d: ", d, "\n";
	z = y;
	a:array[100] integer;
	y = min[a[10]a[20]];
}